<article class="p-4 bg-white border border-gray-200 rounded-lg shadow-sm space-y-2">
  <div class="flex flex-row justify-between items-center">
    <h3 class="text-xl tracking-tight text-gray-900 font-medium">User Review</h3>
    <p class="text-sm text-gray-600">Posted by: <%= review.user.email_address %></p>
        <div class="space-x-2">
          <% if current_user == review.user %>
            <button data-modal-target="edit-review-modal-<%= review.id %>" data-modal-toggle="edit-review-modal-<%= review.id %>" type="button" class="hover:text-gray-800 text-gray-700 text-sm">Edit Review</button>
            <%= link_to "Delete Review", location_review_path(@location, review),
              data: { turbo_method: :delete },
              class: "hover:text-red-700 text-red-500 text-sm" %>
          <% end %>
        </div>  </div>
  <p class="font-normal text-gray-800">
    <%= review.body %>
  </p>
</article>

<div id="edit-review-modal-<%= review.id %>" tabindex="-1" aria-hidden="true" class="hidden overflow-y-auto overflow-x-hidden fixed top-0 right-0 left-0 z-50 justify-center items-center w-full md:inset-0 h-[calc(100%-1rem)] max-h-full">
  <div class="relative p-4 w-full max-w-[800px] max-h-full">
    <div class="relative bg-white rounded-lg shadow-sm">
      <div class="flex items-center justify-between p-4 border-b rounded-t border-gray-200">
        <h2 class="text-lg font-semibold text-gray-900">
          Edit Review
        </h2>

        <button type="button" class="text-gray-600 bg-transparent hover:bg-gray-200 hover:text-gray-900 rounded-lg text-sm w-8 h-8 ms-auto inline-flex justify-center items-center" data-modal-toggle="edit-review-modal-<%= review.id %>">
          <svg class="w-3 h-3" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 14 14">
            <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 1 6 6m0 0 6 6M7 7l6-6M7 7l-6 6"/>
          </svg>
          <span class="sr-only">Close modal</span>
        </button>
      </div>

      <div class="p-4">
        <%= render "reviews/edit_form", review: review, location: review.location %>
      </div>
    </div>
  </div>
</div>
